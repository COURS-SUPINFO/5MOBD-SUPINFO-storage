{
  "name": "mesbonnesadresses-upload-api",
  "version": "1.0.0",
  "private": true,
  "description": "Tiny API that hands out presigned MinIO upload URLs so the app never holds the storage secret key.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.1140.0",
    "@aws-sdk/s3-request-presigner": "^3.1140.0",
    "cors": "^2.8.6",
    "express": "^5.2.1"
  }
}
